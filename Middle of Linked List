class Solution {
    int getMiddle(Node head) {
        // code here
        int lenght=0;
        Node temp=head;
        // to find the length
        while(temp != null){
            temp=temp.next;
            length++;
        }
        
        temp=head;
        for (int i =1;i<=length/2;i++){
            temp=temp.next;
        }
        
        return temp.data;
        
    }
} 
